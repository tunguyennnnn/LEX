apiVersion: v1
kind: Service
metadata:
  name: transriber
spec:
  selector:
    name: transriber
    tier: backend
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: transriber
spec:
  replicas: 1 # number of instances
  template:
    metadata:
      labels:
        name: transriber
        tier: backend
        type: deployment
    spec:
      containers:
      - name: transriber
        image: "gcr.io/lexv-159619/transriber:v0.0.1"
        env:
        - name: PORT
          value: "4000"
        ports:
        - name: http
          containerPort: 4000
