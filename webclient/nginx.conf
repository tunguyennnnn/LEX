include /etc/nginx/conf.d/upstreams/*.conf;

server {
    listen 80;

    location / {
        root html;
        index index.html;
    }

    location ^~ /search/ {
        proxy_pass http://searchengine/;
    }
}
